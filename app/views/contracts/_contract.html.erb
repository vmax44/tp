  <tr>
    <td><%= link_to contract.number, edit_contract_path(contract) %></td>
    <td><%= contract.date %></td>
    <td><%= contract.datestart %></td>
    <td><%= contract.datefinish %></td>
    <td><%= "#{contract.strahovatel.lastname} #{contract.strahovatel.firstname}" %></td>
    <td><%= "#{contract.zastrahovanniy.lastname} #{contract.zastrahovanniy.firstname}" %></td>
    <td><%= contract.cost %></td>
    <td><%= contract.user.ikp %></td>
    <td><%= link_to "Remove", contract, method: :delete, data: { confirm: "Are you sure?" } %></td>
  </tr>