<h1>New Organization</h1>

<div class="row">
  <div>
    <%= form_for(@organization) do |f| %>
      
      <%= render 'fields', f: f %>
      <%= f.submit "Save organization", class: "btn btn-large btn-primary" %>
    <% end %>

  </div>
</div>